import React from 'react';
import './App.css';

const Login = ({admin, canBook, canViewMoney}) => {
    return (
        <div>
            <h1>Log me in</h1>
        </div>
    );
};

export default Login;
