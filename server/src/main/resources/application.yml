application:
  apiToken: ${API_TOKEN}
  workspaceId: ${WORKSPACE_ID}
  security:
    username: test
    password: >-
      {bcrypt}:$2y$12$NngCoy9yx.Cr4r1IHPGaZ.myAHsIxqzU16V92FUnTtxbPddufzt7
    secret: ${TOGGL_SECRET}
    expiration: 28800

management:
  endpoints:
    web:
      exposure:
        include: health, info
  info:
    build:
      enabled: true
    git:
      enabled: true
      mode: full

