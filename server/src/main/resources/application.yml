application:
  apiToken: ${API_TOKEN}
  workspaceId: ${WORKSPACE_ID}
  security:
    username: test
    password: '{bcrypt}$2a$10$UgRG95WPbEoJlc2jNn93HOS5xINfYYFkjt0F6pk1QfV1tvAgAy1a6'
    secret: ${TOGGL_SECRET}
    expiration: 28800

management:
  endpoints:
    web:
      exposure:
        include: health, info
  info:
    build:
      enabled: true
    git:
      enabled: true
      mode: full

